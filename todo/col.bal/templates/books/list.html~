{% extends "base.html" %}

{% block title %}
    {{ title }}
{% endblock %}

{% block content %}
    <h1 id="">Remember the milk</h1>
    </br><h3>Current Datetime:{{datetime}}</h3>
    <ol>
        {% for book in pagination.items %}
            <li>Title: {{ book.title }} &nbsp &nbsp &nbsp  Year: {{ book.year }}&nbsp &nbsp &nbsp Time:{{book.time}} &nbsp &nbsp &nbsp (<a href="{{ url_for('edit_book', id=book.mongo_id) }}">Edit</a> | <a href="{{ url_for('delete_book', id=book.mongo_id) }}">Delete</a>)</li>
        {% endfor %}
    </ol>
    
{% endblock %}
